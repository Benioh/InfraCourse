# 能力矩阵（Patch Track 重写版）

> 这份矩阵回答"学完每关到底能做什么"。每关只有一档：**patch-test PASS = 已掌握**。
> Capstone (L12) 单独有 Bronze / Silver / Gold 三档。

## 全 25 关能力清单

| 关卡 | 学完后你能 | 简历可写 |
|---|---|---|
| **L01** | 精确拆分任意模型显存四件套（params / grads / optimizer / activations） | "能定量回答 7B Adam 显存预算" |
| **L01.5** | 手写 PyTorch DDP grad all-reduce + 平均 | "理解 DDP 数学合同（grad 取均不是和）" |
| **L01.7** | 写 Triton 数值稳定 softmax kernel | "懂 GPU 内存层级 / online softmax / FlashAttention 基础" |
| **L02** | 手写 Tensor Parallel Linear（Column + Row + bias 处理） | "能改 Megatron TP layers，知道 forward/backward 通信位置" |
| **L03** | 写 selective activation checkpoint policy | "懂 TorchTitan / Megatron 的 ckpt 策略接口" |
| **L04** | 给训练框架加新 LR scheduler（含热重启） | "能扩展 Megatron `optimizer_param_scheduler`" |
| **L04.5** | 写 ring attention forward（online softmax 分块） | "理解 FlashAttention Algorithm 1，长上下文显存 O(N)" |
| **L04.8** | 实现 Megatron-shaped train step | "能讲清 forward/backward、optimizer、scheduler 的训练步边界" |
| **L05** | 写 bucketed grad all-reduce（合并通信） | "能调 PyTorch DDP `bucket_cap_mb` 和 ZeRO-1" |
| **L05.5** | 实现 MoE top-2 router + capacity factor + Switch aux loss | "能给 Megatron MoE 加路由策略，调负载均衡" |
| **L05.8** | 实现 distributed checkpoint save/load 与 parallel_state 校验 | "能定位 Megatron checkpoint/resume mismatch" |
| **L06 ★** | 写多模态 collator（image+audio+text 变长打包） | "做过 LLaVA/Qwen2-VL 风格数据管道" |
| **L06.3** | 写 MinHash 文本去重（含 Jaccard 估计） | "懂大模型预训练去重 pipeline（RedPajama / Dolma）" |
| **L07** | 给 vLLM-shape 引擎加 typical_p sampling | "能扩展推理引擎采样策略（top_p / typical_p / min_p）" |
| **L07.5** | 写 vLLM scheduler + KV block 单步调度模型 | "能解释 TTFT/ITL 与 KV block pressure 的边界" |
| **L08** | 写 RadixCache 前缀缓存 trie | "理解 SGLang prefix cache + OpenAI prompt caching 原理" |
| **L08.5** | 写 AWQ-lite per-channel W8 量化 | "懂 W8/W4 量化数学，能选 PTQ vs QAT" |
| **L08.7** | 写 greedy spec decode verify | "理解 Speculative Decoding / Medusa / EAGLE 加速比公式" |
| **L09** | 写 Prometheus metrics exporter（gauge/counter/hit_rate） | "能给推理服务加观测指标，调试 P99 latency" |
| **L09.5** | 写 SGLang PD route + KV transfer service | "能解释 PD 分离中的 worker load 与 KV transfer 指标" |
| **L10** | 写 PPO adaptive KL controller | "懂 RLHF 中 KL penalty 自动调节，防 reward hacking" |
| **L10.5** | 写带并发上限的 async rollout pool | "理解 RL 训练 / 推理解耦架构（SLiME / Mooncake）" |
| **L11** | 写训练 ↔ 推理 weight sync coordinator | "能搭跨集群 NCCL P2P weight sync，处理 shape/dtype 校验" |
| **L11.5** | 写 versioned RolloutManager | "能解释 actor/rollout 权重新鲜度与 RL 稳定性的关系" |
| **L12 ★★** | 把 Qwen2.5-0.5B 改成 image+audio omni 模型，三阶段交付 | "做过完整多模态 LLM 工程化部署（LLaVA-style）" |

## 全课能力地图（按主题归类）

```
显存 & 性能
  L01 显存账本 / L05 bucket / L08.5 量化 / L01.7 Triton softmax / L04.5 ring attention

并行训练
  L01.5 DDP / L02 TP / L05.8 parallel state+ckpt / L05.5 MoE / L11 weight sync / L05 ZeRO 知识

训练动力学
  L03 ckpt policy / L04 LR scheduler / L04.8 Megatron lifecycle / L10 KL control

推理服务
  L07 typical_p / L07.5 vLLM scheduler+KV / L08 RadixCache / L08.5 AWQ / L08.7 spec decode / L09 metrics / L09.5 SGLang PD

RL 对齐
  L10 KL ctrl / L10.5 rollout / L11 weight sync / L11.5 freshness

数据
  L06 多模态 collator / L06.3 dedup

★ 多模态全链路
  L01.5/L02/L05 训练 → L03/L04 调度 → L06 数据 → L07/L08/L08.5/L08.7/L09 服务
  → L10/L10.5/L11 RL → L12 集成
```

## Capstone (L12) 三档评分

| 维度 | Bronze | Silver | Gold |
|---|---|---|---|
| **Stage A** projector 训练 | patch-test 7/7 PASS | CLIP score > 0.20，WER < 0.5 | CLIP > 0.25，WER < 0.3 |
| **Stage B** OpenAI endpoint | endpoint 能返图像 caption | image / audio / 混合三模态都支持 | TTFT < 500ms@H100，prefix_cache_hit > 0.3 |
| **Stage C** RL 对齐 | reward 跑通无 NaN | reward 上升、KL 稳定 | post-RL CLIP score / WER 提升 ≥ 5pp |
| **报告** | — | 5 页报告含 3 个真改过的 framework diff | 8 页 + 架构图 + "下一个月做什么" |

## 简历完整段落（Gold 级别）

> **Multimodal Infra Engineer · Self-led project (2026)**
>
> 完整重构 16+ 个深度学习基础设施组件并集成为多模态 omni 模型：
> - 训练栈：手写 Tensor Parallel Linear、bucketed DDP、selective activation checkpoint、
>   cosine warm restart LR scheduler、ring attention forward (online softmax)、
>   MoE top-2 router (Switch-style aux loss)。
> - 数据栈：变长 image+audio+text 多模态 collator、MinHash + LSH 去重。
> - 推理栈：Locally Typical Sampling filter、SGLang-style RadixCache 前缀缓存（含 LRU evict）、
>   AWQ-lite W8 per-channel 量化、Greedy speculative decoding verify、
>   Prometheus exposition exporter。
> - RL 栈：InstructGPT-style adaptive KL controller、async bounded-concurrency rollout pool、
>   train↔inference weight sync coordinator。
> - 集成：将 Qwen2.5-0.5B 改造为 image+audio+text omni 模型，
>   通过 OpenAI Chat Completions 兼容 endpoint 部署，
>   并用 PPO + multi-task reward (CLIP score + WER) RL 对齐。
> - 工程指标：TTFT < 500ms (H100)，prefix cache hit rate > 0.3，post-RL +5pp 提升。

## 自检 checklist

每关结束前问自己：

- [ ] 我能不看 starter 重写一遍 patch 吗？
- [ ] 我能给"刚学完的同学"用 5 句话讲清核心机制吗？
- [ ] 我能找出 `github_repo/<framework>/` 真实工程版与我的 patch 的 3 处差异吗？
- [ ] quiz 5 题我能不查资料全对吗？
- [ ] 我能通过 `docs/AI框架理解评估指南.md` 的五层 AI 口试吗？

四项都打勾 = 真学会了。
